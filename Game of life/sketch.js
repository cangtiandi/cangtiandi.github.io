// Game of Life

let grid; 
let gridSize = 10;

function setup() {
  createCanvas(windowWidth, windowHeight);
  grid = displayGrid(gridSize,gridSize);
}

function draw() {
  background(220);
}

function displayGrid(rows, cols){
  let x = 0;
  let y = 0;
  for (let i=0; i<gridSize; i++) {

  }
}